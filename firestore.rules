rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Simple: just require any authenticated user
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}